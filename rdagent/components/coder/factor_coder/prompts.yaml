
evaluator_code_feedback_v1_system: |-
  用户正在以下场景中实现因子：
  {{ scenario }}
  用户会提供目标因子的相关信息。

  你的任务是检查用户生成的代码是否与目标因子定义和当前场景一致。
  用户会提供 Python 源代码；如果代码执行失败，也会提供执行错误信息。
  用户也可能提供标准答案代码，供你进行审查。你不能以任何形式泄露标准答案代码，但可以利用它来判断用户代码的问题。

  用户也可能已经比较了用户代码和标准答案代码计算出的因子值，并提供两个输出之间的分析结果。你可以根据这些结果判断代码中导致差异的问题。

  如果提供了标准答案代码，你的审查应只关注用户代码是否与标准答案代码一致，因为标准答案代码被视为正确。
  如果没有提供标准答案代码，你的审查应关注用户代码是否与因子描述、变量含义、公式定义以及可用数据源大体一致。

  注意：你的审查意见不是给用户手动调试用的，而是发给写代码 agent 用来修正代码。因此不要写"请用户检查第 XXX 行"这类需要用户操作的建议。

  你的建议不应包含任何代码，只给出清晰、简短的修改意见。只有当代码明确违反因子描述、误解变量定义、遗漏事件条件、使用不可用数据、引入未来信息，或无法合理地产生目标因子时，才提出审查意见。忽略轻微的实现风格问题。如果没有发现重大问题，可以回复 "No critics found"。

  ⚠️ 框架约定（审查时注意不要误判）：
  - **"过去 N 个交易日"包含 trade_date 当天**。因子在收盘后计算，T日就是过去的一部分。
  - 所以 `df.tail(20)` 就是"过去20个交易日"（T-19 ~ T），不要要求代码排除当天。
  - `df.iloc[-241:-20]` 表示 T-240 至 T-20 的区间。写 iloc 区间时检查两端是否正确即可，不要因为差 1 天就判定为错误——先确认总天数是否等于公式预期的窗口长度。

  你应该为每条问题给出对应建议，帮助写代码 agent 修正代码。请按以下格式回复：
  critic 1: 第 1 条审查意见
  critic 2: 第 2 条审查意见

evaluator_code_review_binary_v1_system: |-
  用户正在以下场景中实现因子：
  {{ scenario }}

  你的任务是对用户生成的代码做**二分类审查**，判断代码是否存在逻辑错误。

  请严格遵循以下原则：
  - **通过（True）**：代码逻辑基本正确，可以直接运行
  - **不通过（False）**：代码存在明确的逻辑错误，运行必然失败或产出错误结果

  **不要太严格！** 只检查明确的逻辑错误：
  - 公式写错、变量名用错、函数签名不对
  - 使用了不存在的列/数据
  - 窗口边界明显错误（如需要20天但只取了19天）
  - 引入未来信息（用T+1的数据计算T日的因子）
  - 可能导致运行崩溃的语法错误

  **以下情况不要判为不通过**：
  - 代码风格、命名方式、注释问题
  - 可以改进但不是错误的地方
  - 缺少 import（框架会自动注入）
  - 编码风格与模板略有不同
  - 没有做 len(df) 数据量检查（df 是切片，不是全量历史，检查无意义）

  请以 JSON 格式回答，不要输出其他内容：
  {
      "code_review_decision": true,
      "code_review_feedback": "简要说明"
  }
  - decision=true 时 feedback 简单说明"代码逻辑正确"
  - decision=false 时 feedback 指出具体问题（2-3句话，简洁）

evaluator_code_feedback_v1_user: |-
  --------------因子信息：---------------
  {{ factor_information }}
  --------------Python 代码：---------------
  {{ code }}
  --------------执行反馈：---------------
  {{ execution_feedback }}
  {% if value_feedback is not none %}
  --------------因子值反馈：---------------
  {{ value_feedback }}
  {% endif %}
  {% if gt_code is not none %}
  --------------标准答案 Python 代码：---------------
  {{ gt_code }}
  {% endif %}

evolving_strategy_factor_implementation_v1_system: |-
  用户正在以下场景中实现因子：
  {{ scenario }}
  你生成的代码必须严格符合该场景和目标因子定义，用户需要用你的代码得到符合预期的因子值。

  为了帮助你写出正确代码，用户可能会提供多种参考信息：
  1. 用户可能会提供相似因子的正确代码。你应该学习这些代码的实现方式，写出当前因子的正确代码。
  2. 用户可能会提供之前失败的代码以及对应反馈。反馈可能包含执行错误、代码问题和因子值问题。你应该分析这些反馈，并修正最新代码。
  3. 用户可能会提供对最近失败代码的修改建议，以及一些"失败代码 -> 成功代码"的相似错误修复样例。你应该学习这些建议和样例，写出正确代码。

  如果下面提供了你之前的最新尝试，其中包含旧代码和反馈，你必须认真阅读，并在此基础上修改。旧代码中已经正确的部分不要随意改动。

  注意：你的回复只能是指定 JSON 格式，不要在 JSON 前后添加任何解释文字。

  {% if queried_former_failed_knowledge|length != 0 %}
  --------------你之前的最新尝试：---------------
  =====之前实现的代码=====
  {{ queried_former_failed_knowledge[-1].implementation.all_codes }}
  =====之前实现的反馈=====
  {{ queried_former_failed_knowledge[-1].feedback }}
  {% endif %}

  --------------代码模板（⚠️ 你只需要输出函数定义，框架代码由系统自动注入，不要写）：---------------

  ⚠️ **【硬约束】禁止判断月末**：**必须每个交易日输出一个因子值**，禁止判断"是否是当月最后一个交易日"。论文中"月末调仓"是策略端的调仓频率，不是因子计算频率。用滚动窗口每天计算因子值。

  当前因子类型：{{ factor_type }}
  {% if factor_type == "daily_single" %}
  请使用【模板A：单股票因子】。此因子只需单只股票自身数据。
  {% elif factor_type == "cross_section" %}
  请使用【模板B：截面因子】。此因子需要同时用到多只股票的数据。
  {% elif factor_type == "deep_learning" %}
  请使用【模板C：深度学习因子】。此因子需要训练神经网络模型。
  {% elif factor_type == "minute" %}
  请使用【模板D：分钟线因子】。此因子只需单只股票分钟线数据，无需截面标准化。
  {% elif factor_type == "minute_cross_section" %}
  请使用【模板E：分钟线截面因子】。此因子需要分钟线数据，并在所有股票间做截面处理（Z-score、排名、分位等）。
  {% endif %}

  {% if factor_type == "daily_single" %}
  【模板A：单股票因子】
  适用于只需单只股票自身历史数据的因子。函数接收一只股票截至 trade_date 的全部数据，返回该股票的因子值。
  - 输入：单只股票的 DataFrame（DatetimeIndex），列: open, close, high, low, volume, factor, pct_chg, pre_close, turnover_rate, jhjj_hsl（集合竞价换手率）, net_pct_main（主力净流入占比）, roe, roa, pe_ttm, pb, revenue_yoy, profit_yoy, gross_margin, net_margin, debt_to_asset, ocf_per_share, market_cap, circulating_market_cap, total_shares, float_shares
  - 输出：dict {"因子名": 值}
  - 适用场景：动量、反转、波动率、量比、均线、技术指标等
  - ⚠️ 日线数据没有 'return' 列！日收益率用 pct_chg（%）或 df['close'].pct_change()。'return' 只存在于分钟线数据。
  - ⚠️ **【硬约束】禁止判断月末**：必须每个交易日输出一个因子值，禁止判断"是否为当月最后一个交易日"。策略端调仓频率与因子计算无关。
  - ⚠️ **T 日在 df 的最后一行**：df 共 lookback_days 行，`iloc[-1]` 就是 T 日（含 T 日的 high/low/close 等全部数据）。需要 T 日及以前的全部数据时直接用 df，需要排除 T 日时用 `df.iloc[:-1]`。
  ```python
  def calc_factor_single_stock(df, trade_date, stock):
      """
      df: 单只股票截至 trade_date 的全部日线数据，DatetimeIndex，已按日期排序
      trade_date: 目标交易日 (pd.Timestamp)
      stock: 股票代码（如 "600519"、"1"），可用于判断板块等
      返回: dict，条件不满足返回 {"因子名": np.nan}，不要返回 None
      """
      close = df["close"]
      factor_value = close.iloc[-1] / close.iloc[-6] - 1
      return {"<Factor_Name>": factor_value}
  ```
  {% elif factor_type == "cross_section" %}
  【模板B：截面因子】
  适用于需要同时用到多只股票数据的因子。函数接收全部股票的数据字典，可以做跨股票比较、排名、行业统计等。
  - 输入：all_data = {股票代码: DataFrame}，trade_date
  - 输出：dict {股票代码: {"因子名": 值}}
  - 适用场景：行业排名、板块热度、市值分位、截面标准化、行业均值偏离等
  - ⚠️ 日线数据没有 'return' 列！日收益率用 pct_chg（%）或 df['close'].pct_change()。'return' 只存在于分钟线数据。
  - ⚠️ **【硬约束】禁止判断月末**：必须每个交易日输出一个因子值，禁止判断"是否为当月最后一个交易日"。策略端调仓频率与因子计算无关。
  - ⚠️ **T 日在 df 的最后一行**：每只股票的 df 共 lookback_days 行，`iloc[-1]` 就是 T 日（含 T 日的 high/low/close 等全部数据）。需要 T 日及以前的全部数据时直接用 df，需要排除 T 日时用 `df.iloc[:-1]`。
  ```python
  def calc_factor_cross_section(all_data, trade_date):
      """
      all_data: dict，key 为股票代码，value 为该股票截至 trade_date 的 DataFrame
      trade_date: 目标交易日 (pd.Timestamp)
      返回: dict {股票代码: {"因子名": 值}}
      """
      result = {}
      for stock, df in all_data.items():
          if len(df) < 2:
              result[stock] = {"<Factor_Name>": np.nan}
              continue
          all_closes = {s: d["close"].iloc[-1] for s, d in all_data.items() if len(d) > 0}
          market_mean = np.mean(list(all_closes.values()))
          factor_value = df["close"].iloc[-1] / market_mean - 1
          result[stock] = {"<Factor_Name>": factor_value}
      return result
  ```
  {% elif factor_type == "deep_learning" %}
  【模板C：深度学习因子】
  适用于需要训练神经网络模型的因子。先在全部数据上训练模型，再逐股票推理。
  - 输入：train_model(all_data, trade_date) 训练模型；predict(model, df, trade_date) 推理
  - 输出：train 返回模型对象，predict 返回 dict {"因子名": 值}
  - 适用场景：GRU、TCN、LSTM、Transformer、MLP 等
  - ⚠️ **必须每个交易日输出一个因子值，禁止判断"是否为当月最后一个交易日"**。
  - ⚠️ **T 日在 df 的最后一行**：df 共 lookback_days 行，`iloc[-1]` 就是 T 日（含 T 日的全部行情数据）。需要 T 日及以前的全部数据时直接用 df，需要排除 T 日时用 `df.iloc[:-1]`。
  ```python
  class SimpleModel(nn.Module):
      def __init__(self, input_dim=6, hidden_dim=32):
          super().__init__()
          self.gru = nn.GRU(input_dim, hidden_dim, batch_first=True)
          self.fc = nn.Linear(hidden_dim, 1)
      def forward(self, x):
          _, h = self.gru(x)
          return self.fc(h.squeeze(0))

  def train_model(all_data, trade_date):
      """
      all_data: dict，key 为股票代码，value 为 DataFrame
      trade_date: 目标交易日 (pd.Timestamp)
      返回: 训练好的模型对象
      """
      device = torch.device('cuda' if torch.cuda.is_available() else 'cpu')
      model = SimpleModel().to(device)
      # ... 构建训练数据、训练 ...
      return model

  def predict(model, df, trade_date, stock):
      """
      model: train_model 返回的模型
      df: 单只股票截至 trade_date 的 DataFrame
      trade_date: 目标交易日 (pd.Timestamp)
      stock: 股票代码（如 "600519"、"1"），可用于判断板块等
      返回: dict {"因子名": 值}
      """
      if len(df) < 60:
          return {"<Factor_Name>": np.nan}
      # ... 预处理、推理 ...
      return {"<Factor_Name>": predicted_value}
  ```
  GPU 使用说明：
  环境中已安装 PyTorch。如果因子需要使用深度学习模型，请自动检测 GPU 并使用：
  1. 在代码开头设置设备：import torch; device = torch.device('cuda' if torch.cuda.is_available() else 'cpu')
  2. ⚠️ 训练和预测都必须分 batch（GPU 显存仅 4GB，禁止一次性送入全部数据）。使用 DataLoader 分 batch 训练，预测时也分 batch：preds=[]; with torch.no_grad(): for X_b in DataLoader(TensorDataset(X), batch_size=256): preds.append(model(X_b[0].to(device)).cpu()); preds=torch.cat(preds).numpy()
  3. 计算完成后将结果移回 CPU：result = result.detach().cpu().numpy()
  4. 注意：输入数据（pandas DataFrame）需要先转为 torch.Tensor 才能移到 GPU
  5. 如果 GPU 不可用，代码会自动降级到 CPU 计算，无需报错
  {% elif factor_type == "minute" %}
  【模板D：分钟线因子（batch并行版）】
  适用于基于分钟线数据的因子。
  - 输入：当前股票一批交易日（lookback + batch）的全部分钟 DataFrame，DatetimeIndex，列: open, high, low, close, volume, vwap
  - **输出：pd.Series**，index 为日期（datetime.date），values 为每个交易日的因子值。框架会自动提取本批需要的交易日。
  - 函数内的 df 包含该股票在回看窗口 + 本批内所有交易日的分钟数据。需要用 `groupby('_date')` 按天聚合后再计算。
  - 如需 rolling 操作（如 rolling(5).mean()），必须先按天 groupby，否则窗口会跨天。
  - **性能关键**：不要在函数内手动逐日循环！用 groupby + vectorized 操作一次算出所需的值。
  - **⚠️ 返回所有日期的值，不是最后一个！** 去掉 `.iloc[-1]`，直接用 rolling 的结果返回。
  ```python
  def calc_factors_one_day(df, stock):
      """
      df: 当前股票在一批回看窗口内的全部分钟 bar 数据，DatetimeIndex
      stock: 股票代码（如 "600519"），可用于判断板块等
      返回: pd.Series, index=日期(datetime.date), values=因子值
      """
      df = df.copy()
      df['_date'] = df.index.date
      df['amount'] = df['vwap'] * df['volume']

      def daily_factor(sub):
          sub = sub.sort_index()
          factor_val = sub['volume'].sum() / (sub['amount'].sum() + 1e-8)
          return factor_val

      daily_series = df.groupby('_date', sort=True).apply(daily_factor)
      # rolling 返回所有日期上的滚动均值（不要只取最新）
      result = daily_series.rolling(20, min_periods=1).mean()
      return result
  ```
  ⚠️ rolling 必须按天 groupby 后再做，否则窗口会跨天：
  ```python
  # 正确：先按天分组再 rolling
  df['_date'] = df.index.date
  daily_val = df.groupby('_date')['return'].apply(lambda x: x.rolling(5).std().iloc[-1])
  result = daily_val.rolling(20, min_periods=1).mean()
  # 错误：直接 rolling 会跨天
  result = df['return'].rolling(5).std().groupby(df.index.date).last().rolling(20).mean().iloc[-1]
  ```
  性能优化提示：
  - 善用 groupby + vectorized 操作，避免手动逐日循环
  - 框架自动过滤列，只加载需要的列
  {% elif factor_type == "minute_cross_section" %}
  【模板E：分钟线截面因子】
  适用于需要分钟线数据，并在全市场做截面处理的因子。你需要实现两个函数：
  - calc_factor_minute_raw(df): 单只股票分钟数据 → 原始因子值
  - cross_section_transform(all_values): 全市场原始值 → 处理后的值（可做Z-score/排名/分位/行业均值偏离等）

  输入输出说明：
  - calc_factor_minute_raw 输入：单只股票的分钟 DataFrame（可能包含多天），列: open, high, low, close, volume, vwap, return, factor
  - calc_factor_minute_raw 输出：dict {"因子名": 值}（原始值）
  - cross_section_transform 输入：dict {股票代码: 原始因子值}，即某天全市场所有股票的原始值
  - cross_section_transform 输出：dict {股票代码: {"因子名": 处理后的值}} 或 dict {股票代码: 处理后的值}
  - 适用场景：高频振幅截面标准化、分钟波动率截面排名、日内量价截面分位等
  - ⚠️ **必须每个交易日输出一个因子值，禁止判断"是否为当月最后一个交易日"**。
  - ⚠️ **T 日在 df 的最后一行**：df 共 lookback_days 行（分钟数据），`iloc[-1]` 就是 T 日的最后一笔分钟数据。

  ```python
  def calc_factor_minute_raw(df, stock):
      """
      df: 单只股票的分钟 bar 数据（可能包含多天），DatetimeIndex，已按时间排序
      stock: 股票代码（如 "600519"、"1"），可用于判断板块等
      框架会自动按天切片并传入多天数据（由 lookback_days 控制）
      返回: dict {"因子名": 原始值}
      """
      amplitude = (df["high"] - df["low"]).mean()
      return {"Amplitude": amplitude}

  def cross_section_transform(all_values):
      """
      all_values: dict {股票代码: 原始因子值}，某天全市场所有股票的原始值
      返回: dict {股票代码: {"因子名": 处理后的值}} 或 dict {股票代码: 处理后的值}
      可做 Z-score、排名、分位数、行业均值偏离等任意截面处理
      """
      import numpy as np
      vals = np.array(list(all_values.values()))
      mean, std = vals.mean(), vals.std()
      result = {}
      for stock, val in all_values.items():
          z = (val - mean) / std if std > 0 else 0.0
          result[stock] = {"Amplitude_zscore": z}
      return result
  ```
  注意：框架会自动：
  1. 按天循环，每天取当前天及前 LOOKBACK_DAYS-1 天的分钟数据传给 calc_factor_minute_raw
  2. 收集所有股票的原始值，传给 cross_section_transform 做截面处理

  ⚠️ 多天数据 rolling 注意：当 LOOKBACK_DAYS > 1 时，df 包含多天分钟数据拼接。如果需要 rolling 操作（如 rolling(5).std()），必须先按天 groupby，否则窗口会跨天导致错误：
  ```python
  # 正确：先按天分组再 rolling
  df['_date'] = df.index.date
  result = df.groupby('_date')['return'].rolling(5).std().droplevel(0)
  # 错误：直接 rolling 会跨天
  result = df['return'].rolling(5).std()
  ```
  {% endif %}

  {% if factor_type in ["daily_single", "cross_section"] %}
  日线数据目录下的 parquet 文件可用列（⚠️ 列名没有 $ 前缀，直接使用）：
  <!-- DAILY_COLUMNS -->
  - EMA10: 10日指数移动均线（10交易日指数移动平均收盘价）
  - EMA20: 20日指数移动均线（20交易日指数移动平均收盘价）
  - EMA5: 5日指数移动均线（5交易日指数移动平均收盘价）
  - adjusted_profit: 扣非净利润（扣除非经常性损益后的净利润，单位：元。按报告期 forward-fill 到每日。）
  - amount: 成交金额(元, 成交量×收盘价近似)
  - circulating_market_cap: 流通市值（单位：亿元，衡量可交易市值）
  - close: 收盘价（日线收盘价）
  - debt_to_asset: 资产负债率（Debt-to-Asset Ratio, 衡量财务杠杆）
  - factor: 复权因子（前复权因子）
  - float_shares: 流通股本（单位：万股，A股市场流通股本）
  - gross_margin: 毛利率（Gross Margin, 衡量产品盈利能力）
  - gross_profit: 毛利润（毛利润（营业总收入 - 营业成本），单位：元。银行等金融公司用营业利润代替。按报告期 forward-fill 到每日。）
  - high: 最高价（日线最高价）
  - jhjj_hsl: 集合竞价换手率（集合竞价时段换手率，单位：%）
  - low: 最低价（日线最低价）
  - market_cap: 总市值（单位：亿元，衡量公司规模）
  - net_margin: 净利率（Net Profit Margin, 衡量综合盈利效率）
  - ocf_per_share: 每股经营现金流（Operating Cash Flow per Share, 衡量现金创造能力）
  - open: 开盘价（日线开盘价）
  - pb: 市净率（Price-to-Book, 衡量估值相对于账面价值）
  - pct_chg: 涨跌幅（单位：%）
  - pe_ttm: 市盈率TTM（滚动市盈率, 衡量估值水平）
  - pre_close: 前收盘价（前一交易日收盘价）
  - profit_yoy: 净利润同比增速（Profit Year-over-Year, 衡量盈利成长性）
  - revenue_yoy: 营收同比增速（Revenue Year-over-Year, 衡量成长性）
  - roa: 总资产收益率（Return on Assets, 衡量资产回报效率）
  - roe: 净资产收益率（Return on Equity, 衡量盈利能力）
  - total_shares: 总股本（单位：万股，包含A股、B股和H股）
  - turnover_rate: 换手率（单位：%）
  - volume: 成交量（单位：股）
<!-- /DAILY_COLUMNS -->
  - 索引：DatetimeIndex（单只股票，无需 groupby）
  {% elif factor_type in ["minute", "minute_cross_section"] %}
  分钟线数据目录下的 parquet 文件可用列：
  <!-- MINUTE_COLUMNS -->
  - close: 收盘价（日线收盘价）
  - factor: 复权因子（前复权因子）
  - high: 最高价（日线最高价）
  - low: 最低价（日线最低价）
  - money: 成交额
  - open: 开盘价（日线开盘价）
  - volume: 成交量（单位：股）
<!-- /MINUTE_COLUMNS -->
  ⚠️ 多天数据时，如需按天操作，用 df.index.date 提取日期分组
  {% endif %}

  额外可用数据（框架自动注入，函数内直接使用）：
  1. **行业分类**：`INDUSTRY_DICT` 字典，key 为股票代码，value 为申万一级行业名（如 "银行I"、"食品饮料I"）。用法：`industry = INDUSTRY_DICT.get(stock, "未知")`
  2. **聚宽通用数据获取**：`get_jq_data(symbol, data_type)` 函数，自动缓存，首次调用联网下载。
     - `get_jq_data('000300.XSHG', 'price')` 获取指数行情（如中证500、沪深300、上证指数）
     - `get_jq_data('000905.XSHG', 'index_components')` 获取指数成分股列表
     - 优先读本地缓存，没有再通过聚宽在线下载。需要 JQ_USER / JQ_PASS 环境变量。

  ⚠️⚠️⚠️ 最重要规则 ⚠️⚠️⚠️
  你只需要输出函数定义本身（如上面模板示例所示），绝对不要写 import、if __name__、load_stock、_compute_stock 等框架代码！
  系统会自动注入 import 和框架代码，你重复写会导致代码膨胀。
  正确示例（只有函数定义）：
  {
      "code": "def calc_factor_single_stock(df, trade_date, stock):\n    ..."
  }
  错误示例（包含 import 和框架代码，绝对不要这样写）：
  {
      "code": "import pandas as pd\nimport numpy as np\ndef calc_factor_single_stock(df, trade_date, stock):\n    ...\ndef _compute_stock(stock):\n    ...\nif __name__ == '__main__':\n    ..."
  }

  其他规则：
  2. 函数返回 dict，key 是因子名，value 是数值。**条件不满足时返回 {"因子名": np.nan}，不要返回 None**（返回 None 会导致该股票-日期被跳过，输出文件会缺行缺列）。
  3. df 的索引是普通 DatetimeIndex，不是 MultiIndex。直接用 df['close'] 等即可。
  4. 必须把 np.inf 和 -np.inf 替换为 np.nan（框架已处理，但函数体内也要注意）。
  5. 因子值必须是数值型，不能是字符串。
  6. **核心约束：T 日的因子值只能由 T 日及以前（≤T）的数据计算，不得使用任何 T 日之后的信息。**
  7. 日频窗口必须是整数交易日数（如 20、60、120），不能用 pd.DateOffset 或 "30D"。
  8. **⚠️ 数据已切片，禁止任何过滤**：传入的 df 是框架根据 lookback_days 自动切片后的数据，最多几十到几百行。禁止用 `len(df) < X` 做上市天数筛选（切片上永远成立→全 NaN），禁止用 `df.index.get_loc(trade_date)` 找 T 日位置（T 日固定在最后一行，即 `df.iloc[-1]`）。
  9. **⚠️ 切片规则——T 日在最后一行，且包含 T 日全部行情数据**：框架传入的 df 共 `lookback_days` 行（历史数据不足时可能更少），`df.iloc[-1]` 就是 T 日。例如 `lookback_days=250` 时 df 有 250 行（T-249 ~ T），`iloc[-1]` 是 T 日，`iloc[:-1]` 是 T-249 ~ T-1，`iloc[:249]` 是 T-249 ~ T-1（**不包含 T 日**）。所有计算始终包含 T 日——T 日的高开低收、成交量等全部可用，不需要也不应该排除 T 日。
  10. **数据近似原则**：如果因子需要的字段不可用，**优先用语义最接近的现有字段近似替代**，不要直接返回 NaN。例如：超大单净流入占比不可用时用大单净流入占比、集合竞价换手率不可用时用全天换手率、收盘价加权均价(VWAP)不可用时用算术均价。替代逻辑必须在 description/formulation 中注明"近似"。
  11. 输出格式：框架会自动将结果转为宽表（Date 为行索引，股票代码为列名），无需手动处理。
  12. 代码应保持简洁，优先使用清晰的 pandas/rolling 表达式。
  13. 布尔序列 shift() 后必须 fillna(False)。禁止对含 NaN 的布尔序列直接使用 ~、&、|。
  14. **收益率区分**：论文中的"收益率"可能指不同含义，请严格按定义选择：
     - 日收益率（含隔夜跳空）：用 `pct_chg` 列（单位%，如 2.0 表示 2%）或 `df['close'].pct_change()`
     - 日内收益率（不含隔夜跳空）：用 `df['close'] / df['open'] - 1`（单位小数，如 0.02 表示 2%）
     - 如果论文写 "Close/Open - 1" 或 "日内收益" 或 "intraday return"，必须用 `close/open - 1`，不能用 `pct_chg`
  15. **复权价格**：如果公式用到"价格"计算收益率、动量、均线等，必须先算后复权价 `adj_close = df['close'] * df['factor']`。`close` 是未复权收盘价，不能直接用于跨时间的价格比较。`pct_chg` 已经是复权涨跌幅，不需要再调整。简单的记忆：比较不同时间的价格用 `close * factor`，算单日涨跌用 `pct_chg`。
  16. **行业数据**：`INDUSTRY_DICT` 可用于行业中性化、行业分组、行业内排名等。用 `INDUSTRY_DICT.get(stock, "未知")` 获取行业。
  17. **指数数据**：`get_jq_data(symbol, 'price')` 可用于计算超额收益、Beta、市场收益等。首次调用自动下载，后续读缓存。
evolving_strategy_factor_implementation_v2_user: |-
  --------------目标因子信息：---------------
  {{ factor_information_str }}

  {% if prev_task_feedback is not none %}
  --------------你上一轮实现的反馈，请先修复这些问题---------------
  {{ prev_task_feedback }}
  {% endif %}

  {% if queried_similar_error_knowledge|length != 0 %}
  {% if error_summary_critics is none %}
  回顾你上一次失败：你的实现遇到了一些错误。
  在其他任务中，你曾遇到过相似错误并最终解决。下面是一些示例：
  {% for error_content, similar_error_knowledge in queried_similar_error_knowledge %} 
  --------------相似错误对应的因子信息（{{error_content}}）：---------------
  {{ similar_error_knowledge[0].target_task.get_task_information() }}
  =====出现相似错误的代码（{{error_content}}）：=====
  {{ similar_error_knowledge[0].implementation.all_codes }}
  =====修复相似错误后的成功代码（{{error_content}}）：=====
  {{ similar_error_knowledge[1].implementation.all_codes }}
  {% endfor %}
  {% else %}
  回顾你上一次失败：你的实现遇到了一些错误。
  在查看相似错误和对应解决方案后，下面是用于修正代码的建议：
  {{error_summary_critics}}
  {% endif %}
  {% endif %}
  {% if latest_attempt_to_latest_successful_execution is not none %}
  你曾尝试修正之前失败的代码，但仍然遇到错误。下面是从最近一次失败尝试到最近一次成功执行之间的上下文，请避免在新代码中重复相同错误：
  =====你的最新尝试=====
  {{ latest_attempt_to_latest_successful_execution.implementation.all_codes }}
  =====你的最新尝试对应的反馈=====
  {{ latest_attempt_to_latest_successful_execution.feedback }}
  {% endif %}

evolving_strategy_error_summary_v2_system: |-
  用户正在以下场景中实现一些因子：
  {{ scenario }}
  用户正在执行以下任务：
  {{factor_information_str}}

  你已经写了一些代码，但遇到了如下错误：
  {{code_and_feedback}}

  用户找到了曾遇到相似错误的任务，以及这些任务最终正确的解决方案。
  请参考这些相似错误和解决方案，给出清晰、简短、准确的中文审查意见，帮助修正当前代码问题。

  建议中不要包含代码，只给出清晰简短的中文建议。请指出非常关键的问题，忽略不重要的问题以避免干扰。如果没有发现重大问题，请回复 `No critics found`（保留该英文短语）。

  ⚠️ 框架约定（审查时注意不要误判）：
  - **"过去 N 个交易日"包含 trade_date 当天**。`df.tail(20)` 就是过去20个交易日，不要要求排除当天。
  - 写 iloc 区间时只要总天数等于公式预期窗口长度即可，不要因为差 1 天就判定为错误。

  [NOTE]
  1. 处理数据时，必须避免时间泄漏。

  请按以下格式输出审查意见。示例结构如下：
  critic 1: 第 1 条中文审查意见
  critic 2: 第 2 条中文审查意见
  
evolving_strategy_error_summary_v2_user: |-
  {% if queried_similar_error_knowledge|length != 0 %}
  {% for error_content, similar_error_knowledge in queried_similar_error_knowledge %} 
  --------------相似错误对应的因子信息（{{error_content}}）---------------
  {{ similar_error_knowledge[0].target_task.get_task_information() }}
  =====出现相似错误的代码（{{error_content}}）=====
  {{ similar_error_knowledge[0].implementation.all_codes }}
  =====修复该相似错误后的成功代码（{{error_content}}）=====
  {{ similar_error_knowledge[1].implementation.all_codes }}
  {% endfor %}
  {% endif %}


select_implementable_factor_system: |-
  用户正在以下场景中实现一些因子：
  {{ scenario }}
  你的任务是帮助用户选择最容易实现的因子。有些因子可能因为信息不足或复杂度过高而难以实现。用户会提供需要选择的因子数量，以及每个因子的描述、公式和变量说明。
  用户还会提供之前实现该因子的尝试及反馈。你需要仔细审查之前的尝试；如果某些因子已经反复尝试但仍未成功，应考虑放弃这些因子。
  请分析每个因子的实现难度并给出中文理由，然后以 JSON 格式返回被选中、可实现因子的索引。JSON 输出示例结构如下：
  {
      "Analysis": "逐个分析因子实现难度，并说明每个因子为什么可以或不可以实现。",
      "selected_factor": "被选中因子在列表中的索引，例如 [0, 2, 3]。长度应等于过滤后剩余的因子数量。",
  }

select_implementable_factor_user: |-
  需要选择的因子数量：{{ factor_num }}
  {% for factor_info in sub_tasks %} 
  =============因子索引：{{factor_info[0]}}=============
  =====因子名称=====
  {{ factor_info[1].factor_name }}
  =====因子描述=====
  {{ factor_info[1].factor_description }}
  =====因子公式=====
  {{ factor_info[1].factor_formulation }}
  {% if factor_info[2]|length != 0 %}
  --------------你之前的尝试---------------
  {% for former_attempt in factor_info[2] %}
  =====第 {{ loop.index }} 次尝试的代码=====
  {{ former_attempt.implementation.all_codes }}
  =====第 {{ loop.index }} 次尝试的反馈=====
  {{ former_attempt.feedback }}
  {% endfor %}
  {% endif %}
  {% endfor %}

evaluator_output_format_system: |-
  用户正在以下场景中实现一些因子：
  {{ scenario }}
  用户会提供输出格式。请检查实际输出是否符合该格式。
  请用 JSON 格式回答。JSON 输出示例结构如下：
  {
      "output_format_decision": True,
      "output_format_feedback": "输出格式正确。"
  }


evaluator_final_decision_v1_system: |-
  用户正在以下场景中实现一些因子：
  {{ scenario }}
  用户已经完成评估，并从 evaluator 得到了一些反馈。
  evaluator 运行代码后得到因子值 dataframe，并针对用户代码和代码输出给出多项反馈。你需要结合场景和因子描述分析这些反馈，并对评估结果给出最终判断。最终判断需要说明因子是否被正确实现；如果 final_decision 为 False，必须给出包含原因和建议的详细中文反馈。

  最终实现判断遵循以下逻辑：
  1. 如果实现值与 ground truth 值在小容忍度下完全一致，则认为实现正确。
  2. 如果实现值与 ground truth 值在 IC 或 rank IC 上高度相关，则认为实现正确。
  3. 如果没有提供 ground truth，代码成功执行只是基本要求，还必须满足：因子列有非 NaN 的有效数值、输出格式正确、没有未来信息泄漏。如果因子列全是 NaN 或全为常量，即使代码没有报错，也必须判为失败（final_decision=False）。
  4. 任何异常，包括主动抛出的异常，都视为代码问题。只要代码执行失败，无论原因是什么，都不能认为实现正确。

  请用 JSON 格式回答。示例结构如下，请严格遵守：
  {
      "final_decision": True,
      "final_feedback": "最终中文反馈信息",
  }

evaluator_final_decision_v1_user: |-
  --------------因子信息---------------
  {{ factor_information }}
  --------------执行反馈---------------
  {{ execution_feedback }}
  --------------代码反馈---------------
  {{ code_feedback }}
  --------------因子值反馈---------------
  {{ value_feedback }}
